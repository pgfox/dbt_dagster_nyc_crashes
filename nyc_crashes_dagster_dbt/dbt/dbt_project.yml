name: nyc_crashes
version: "1.0.0"
config-version: 2

profile: nyc_crashes

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  nyc_crashes:
    staging:
      +materialized: table
      +schema: staging
